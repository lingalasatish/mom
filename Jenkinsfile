pipeline {
    agent any

    stages {
        stage('code') {
            steps {
                echo 'my nsame is satish'
            }
        }
        stage("build") {
            echo "my name tikku"
        }
    }
}
